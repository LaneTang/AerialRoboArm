已进入 **MODE C: CODER (Implementation Engineer)** 模式。

根据您提供的 `FOC_DEMO2.ioc` 配置文件 ，我已对系统的引脚分配进行了全面核对与更新。与原 `pinmap.md` 相比，主要新增了 **TIM2 的两路 PWM 输出**（通常用于舵机控制）以及 **USART1 的重映射引脚**。

以下是更新后的**系统全局接线表**：

---

### 1. 电机驱动模块 (SimpleFOC Mini / DRV8313)

*注意：PWM 引脚对应 TIM1 的三个通道，受硬件定时器控制 。*

| STM32 引脚 | 功能定义 | 连接到驱动板 | 备注 |
| :--- | :--- | :--- | :--- |
| **PA8** | TIM1_CH1  | **IN1** | U 相 PWM |
| **PA9** | TIM1_CH2  | **IN2** | V 相 PWM |
| **PA10** | TIM1_CH3  | **IN3** | W 相 PWM |
| **PA11** | GPIO_Output  | **EN** | **使能引脚** (高电平使能) |
| **GND** | 地 | **GND** | **必须共地！** |
| - | - | **VCC** | 接 12V 直流电源 (切勿接STM32!) |

---

### 2. 磁编码器模块 (AS5600)

*注意：使用 I2C1 接口，配置为快速模式 (I2C_Fast) 。*

| STM32 引脚 | 功能定义 | 连接到 AS5600 | 备注 |
| :--- | :--- | :--- | :--- |
| **PB8** | I2C1_SCL  | **SCL** | 时钟线 (硬件 I2C，建议外接 4.7k 上拉) |
| **PB9** | I2C1_SDA  | **SDA** | 数据线 (硬件 I2C，建议外接 4.7k 上拉) |
| **3.3V** | 电源 | **VCC** | AS5600 供电 |
| **GND** | 地 | **GND** | **必须共地** |

---

### 3. 通信与外设接口 (UART & PWM)

*根据 IOC 配置，系统启用了 USART1、USART3 以及 TIM2 的 PWM 通道 。*

| STM32 引脚 | 功能定义 | 连接对象 | 备注 |
| :--- | :--- | :--- | :--- |
| **PB10** | USART3_TX  | 外设/调试 RXD | 异步模式  |
| **PB11** | USART3_RX  | 外设/调试 TXD | 异步模式  |
| **PB6** | USART1_TX  | 外设 RXD | 异步模式，波特率 420000  (常用于 ELRS 等高波特率接收机) |
| **PB7** | USART1_RX  | 外设 TXD | 异步模式  |
| **PA0** | TIM2_CH1  | 舵机/执行器 1 | PWM 生成通道 1  |
| **PA1** | TIM2_CH2  | 舵机/执行器 2 | PWM 生成通道 2  |

---

### 4. 系统供电与调试 (SWD & Clock)

| STM32 引脚 | 功能定义 | 连接对象 | 备注 |
| :--- | :--- | :--- | :--- |
| **PA13** | SYS_JTMS-SWDIO  | 仿真器 SWDIO | 串行调试数据线  |
| **PA14** | SYS_JTCK-SWCLK  | 仿真器 SWCLK | 串行调试时钟线  |
| **PC13** | GPIO_Output  | 板载 LED / RTC | 默认输出低电平  |
| **PD0** | RCC_OSC_IN  | 外部晶振 | 8MHz HSE 输入  |
| **PD1** | RCC_OSC_OUT  | 外部晶振 | HSE 输出  |

---

**更新总结：**
底层硬件抽象层（L1 BSP）需要确保对外暴露接口时，将 `TIM2_CH1` / `TIM2_CH2` 封装为 2DOF 舵机的驱动支持，并将 `USART1` 配置为支持 420000 波特率的通信协议（如 ELRS）解析入口。