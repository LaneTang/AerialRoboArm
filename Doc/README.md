# FOC demo

本项目用于 FOC算法 学习

项目采用 

| 硬件类型 | 选型            |
|------|---------------|
| 主控芯片 | STM32F103C8T6 |
|      |               |

## 项目接线

根据你提供的 STM32F103C8Tx 引脚配置图（STM32CubeMX 截图），并结合我们之前开发的 ARA FOC 项目代码，这里是完整的**系统全局接线表**。

请务必按照此表检查你的硬件连接，特别是 **Enable 引脚** 和 **串口引脚**，因为它们与默认配置可能不同。

---

### 1. 电机驱动模块 (SimpleFOC Mini / DRV8313)

*注意：PWM 引脚对应 TIM1 的三个通道。*

| STM32 引脚 | 功能定义 | 连接到驱动板 (SimpleFOC Mini) | 备注 |
| --- | --- | --- | --- |
| **PA8** | TIM1_CH1 | **IN1** | U 相 PWM |
| **PA9** | TIM1_CH2 | **IN2** | V 相 PWM |
| **PA10** | TIM1_CH3 | **IN3** | W 相 PWM |
| **PA11** | GPIO_Output | **EN** (Enable) | **使能引脚** (注意修改代码定义) |
| **GND** | 地 | **GND** | **必须共地！** |
| - | - | **VM / VCC** | 接 12V 电源 (不要接 STM32!) |

> **⚠️ 代码修改提醒**：
> 你的图片中将 **PA11** 配置为了 GPIO 输出（即使能）。请检查你的 `main.h` 或 `drv_bldc_power.c`，确保宏定义匹配：
> `#define BSP_GPIO_MOTOR_EN  GPIO_PIN_11`
> `#define BSP_PORT_MOTOR_EN  GPIOA`

---

### 2. 磁编码器模块 (AS5600)

*注意：使用 I2C1 接口。*

| STM32 引脚 | 功能定义 | 连接到 AS5600 | 备注 |
| --- | --- | --- | --- |
| **PB8** | I2C1_SCL | **SCL** | 时钟线 (建议接上拉电阻) |
| **PB9** | I2C1_SDA | **SDA** | 数据线 (建议接上拉电阻) |
| **3.3V** | 电源 | **VCC / 3.3V** | AS5600 支持 3.3V 或 5V |
| **GND** | 地 | **GND** | **必须共地** |
| - | - | **DIR** | 悬空或接地 (决定方向) |
| - | - | **GPO** | 悬空 (不使用) |

---

### 3. 串口调试 (USB-TTL / 串口助手)

*注意：图片中使用的是 **USART3** (PB10/PB11)，而不是常见的 USART1 (PA9/PA10 被 PWM 占用了)。*

| STM32 引脚 | 功能定义 | 连接到 USB-TTL 模块 | 备注 |
| --- | --- | --- | --- |
| **PB10** | USART3_TX | **RXD** | STM32 发 -> 电脑收 |
| **PB11** | USART3_RX | **TXD** | STM32 收 <- 电脑发 |
| **GND** | 地 | **GND** | **必须共地** |
| **5V** | 供电 (可选) | **5V** | 如果 STM32 需要 USB 供电 |

---

### 4. 系统供电与调试 (SWD)

| STM32 引脚 | 功能 | 连接对象 | 备注 |
| --- | --- | --- | --- |
| **PA13** | SWDIO | ST-Link / DAPLink | 仿真器数据线 |
| **PA14** | SWCLK | ST-Link / DAPLink | 仿真器时钟线 |
| **PC13** | GPIO_Output | 板载 LED | 通常低电平点亮 (用于状态指示) |
| **NRST** | Reset | 复位按键 | 外部复位 |
| **PD0/PD1** | OSC_IN/OUT | 外部晶振 | 8MHz 晶振 (HSE) |

---

### ✅ 接线检查清单 (Checklist)

1. **共地 (Common Ground)**：这是最重要的一点。STM32 的 GND、驱动板的 GND、AS5600 的 GND、USB转串口的 GND，这四者必须最终连在一起。
2. **Enable 引脚**：图片显示是 **PA11**。请务必确认你的杜邦线是从 PA11 接到了驱动板的 EN 脚。
3. **电源电压**：
* 驱动板 VM 接 12V。
* STM32 和 AS5600 接 3.3V (或通过 USB 5V 经 LDO 降压)。
* **千万不要把 12V 接到 STM32 的任何引脚上，否则瞬间烧毁。**


4. **I2C 上拉**：如果 AS5600 读取不稳定，检查 PB8/PB9 是否在模块上有上拉电阻（通常模块自带，如果是裸芯片则需要加 4.7k 电阻到 3.3V）。

这张表是基于你提供的图片生成的，按照这个接线，配合我们刚才验证通过的 L3 算法代码，新板子一到即可直接工作！

## 5. 通信协议

```md
// 事件类型定义
typedef enum {
    EVT_NONE = 0,
    
    // 输入事件
    EVT_RC_DATA_UPDATE,    // 遥控器通道数据更新 (Group B)
    EVT_RC_SWITCH_MODE,    // 遥控器拨杆切模式 (Manual <-> Auto)
    EVT_VISION_NEW_OBJ,    // 视觉识别到新物体 (Group C)
    EVT_VISION_LOST,       // 视觉丢失目标
    
    // 系统事件
    EVT_MOTOR_ERROR,       // 电机报错
    EVT_MOTION_ARRIVED     // 运动到位
} EventType_t;

// 视觉数据载荷 (压缩存储)
typedef struct {
    uint8_t obj_type;
    int16_t x_offset;      // 相对中心偏差
    int16_t y_offset;
    uint16_t distance_mm;
} Payload_Vision_t;

// 遥控数据载荷
typedef struct {
    int16_t throttle;
    int16_t yaw;
    int16_t pitch;
    int16_t roll;
    uint8_t buttons;       // 位域表示按键
} Payload_RC_t;

// 通用事件包 (Union节省空间)
typedef struct {
    EventType_t type;
    uint32_t timestamp;
    union {
        Payload_Vision_t vision;
        Payload_RC_t rc;
        uint32_t raw_data; // 简单数据
    } content;
} SysEvent_t;
```


## LOG 

### 25/11/17 晚

写个规划

初步完成FOC算法的理论知识，开始尝试**搭建测试项目**进行一次开发验证

1. OLED 屏幕驱动实现
2. （或）屏幕显示LOG任务实现
3. （或）串口收发LOG任务实现

完成上述模块之后，开始实现 FOC算法

特别鸣谢：https://github.com/Bjersgen/Simplefoc

提到的关于STM32与esp32/arduino的硬件差异在simpleFOC部署上导致的性能区别启发了我

### 25.11.28 night

在FOC_DEMO2中搭建了测试平台，测试freeRTOS框架下的I2C/UART bsp代码实现和drv_as5600/drv_bldc代码实现

done：
1. I2C1 DMA tx/rx 代码实现
2. UART3 DMA tx + DMA rx RingBuffer bsp代码实现和功能测试
3. AS5600 drv 代码实现且成功读取到reg数据
   1. I2C1 fast mode 下20cm杜邦线情况会导致直接无法读取reg数据，返回000
   2. 换了更短的杜邦线发现问题解决

undone:
1. BLDC drv 实现与测试
2. FOC mod 实现与测试

### 26.1.19

本次会议主要围绕代码分支问题展开讨论，并对项目整体架构进行了讲解，明确了后续分工。
小结
1. 代码分支与架构问题

Vincent Lu 在克隆代码时遇到了分支问题，无法拉取到最新的代码。
Lane Tang 协助排查并指导其重新拉取正确的分支。
双方确认了项目的整体软件架构，分为四层：BSP（板机支持包）、驱动、模块和Task。

2. 项目整体架构讲解

BSP层: 封装了对底层硬件的调用，包括GPIO、I2C、UART等，并实现了DMA、信号量等机制。
驱动层: 已完成编码器和BLDC电机的驱动开发。
模块层: 由于目标芯片无FPU，采用查找表（LUT）等方式实现了PID、FOC等算法，以降低资源消耗。
Task层: 实现了基于位置环的速度闭环控制。

3. 后续工作分工

Lane Tang 负责软件部分的开发与整合。
Vincent Lu 负责硬件及机械结构部分。

待办
1. 代码与文档整理

Vincent Lu 需要将项目中的三个.h文件发到群里。
Vincent Lu 需要将硬件及机械结构部分的相关资料整理出来。

2. 项目整合与学习

Vincent Lu 需要在会后研究并整合Lane Tang提供的BSP代码。
Vincent Lu 需要在会后研究Lane Tang分享的AI使用方法。


---
本次会议讨论了机械臂抓取测试方案，并分享了多智能体协作系统的实践经验。
小结
1. 机械臂抓取测试方案

测试目标: 在4月份进行测试，使用发球器发射物体，由机械臂在指定时间内完成抓取。
性能要求: 整体响应时间需在0.2秒内，包含目标检测（约0.1秒）和机械臂动作（约0.1秒）。
技术方案: 计划采用“地面端计算”的模式，即无人机将视频流传输至地面PC进行图像处理和决策，再将指令发送给机械臂执行。

2. 多智能体协作系统实践

背景: 介绍了“单体增强流”和“多智能体协作流”两种AI应用模式，后者因上下文丢失等问题成为主流解决方案。
系统设计: 构建了一个三角色（designer, API, code）的多智能体协作系统，模拟软件工程中的分工，由用户作为协调者，简化交互流程。
实现方式: 使用Gemini等AI模型，通过自定义指令和工作流，将不同角色的AI置于独立认知空间内，由用户统一协调。

待办
1. 机械臂抓取测试方案

Vincent Lu需将范子川侧的要求整理成文档并发送至群里。

2. 多智能体协作系统

Lane Tang需将本次分享的多智能体协作系统文档整理并分享。


### 

本次会议主要介绍了多智能体协作系统的使用方法，并演示了通过AI助手创建自定义Agent的完整流程。
小结
1. 多智能体协作系统介绍与推荐

Lane Tang 推荐了一种名为“is yes”语法的方法论，用于更好地向AI提出需求。
强调在多智能体协作系统中，用户的核心职责是明确并提出需求，AI负责执行。

2. 多智能体协作系统的实操演示

会议演示了如何通过一个预设的原生Agent（原Prompt），将模糊的想法孵化为一套成熟的开发指令。
演示流程分为三个阶段：

Phase1：需求确认。通过与AI助手对话，引导用户明确自身需求。
Phase2：架构确认。AI助手根据初步需求，生成初步的技术架构方案供用户确认。
Phase3：创建Agent。将确认后的架构方案导入到指定的AI平台（如“正面”），创建并保存为自定义Agent。



3. AI工具使用与交流

讨论了AI工具的获取方式，提及可通过学生认证等方式获得免费或低成本的AI服务。
分享了个人使用AI工具的经验，例如会使用多个AI工具协同工作，互相审查代码。

待办
1. 分享AI智能体协作系统文档

Lane Tang 会后将AI智能体协作系统的相关文档分享给Vincent Lu。

2. 同步AI工具使用体验与反馈

若团队成员尝试使用AI智能体协作系统，需将体验和反馈同步给Lane Tang。

### 26.2.27

**目标平台**: STM32F103C8T6 (无FPU) + SimpleFOC Mini驱动板 + AS5600编码器 + 2804云台电机

**当前状态**: ✅ 所有问题已解决。系统在速度闭环模式下运行稳定。

Issue 1：进入闭环瞬间触发 RTOS HardFault (一发指令即死机)
- 现象: 发送 c (闭环) 或 t (开环) 指令后，MCU 仅打印一行遥测日志便瞬间死机卡死，对后续指令无反应。
- 根因: LogTask 任务堆栈溢出。CubeMX 默认分配的 128 字 (Words) 堆栈过小。在无硬件 FPU 的 MCU 上，调用处理 %f 浮点格式化的 vsnprintf 函数时，局部变量瞬间耗尽了任务栈，破坏了 RTOS 调度器。
- 修复: 
  1. 在 FreeRTOS 配置中将 LogTask 的堆栈大小 (Stack Size) 增加到 512。

Issue 2：PWM 幽灵断电 (脑电波正常，肌肉瘫痪)
- 现象: 软件运行完美，日志持续刷新，但电机没有任何保持力矩，一动不动。
- 根因: 安全机制导致的硬件锁死。BSP_PWM_StopAll() 在停止时正确清零了高级定时器的 MOE (主输出使能) 位；但后续调用 DrvBldc_Enable(true) 试图恢复电机时，仅更新了占空比寄存器，未重新置位 MOE，导致物理引脚持续处于高阻态。
- 修复: 在 DrvBldc_Enable() 的开启分支中，补充调用 BSP_PWM_Init()，以唤醒硬件并重新激活 MOE 引脚输出。

Issue 3：“蚊子腿”级别的驱动电压 (数值缩放错误)
- 现象: 硬件使能后，电机依然无法转动，甚至无法克服轴承的静态摩擦力。
- 根因: FOC 算法底层采用 Q15 定点数格式 (满量程 32767 = 100% 占空比)。此前代码中设定的测试电压 Uq = 400，换算后仅占 1.2% 的占空比 (在 12V 供电下仅有约 0.14V)，驱动力微乎其微。
- 修复: 在 test_console.c 中全面放大测试基准参数。将零点对齐电压修改为 4000，开环转矩模式电压修改为 3000。

Issue 4：正反馈死锁 与 PID 积分限幅过小
- 现象: 在 t 模式下，电机暴力锁死在固定角度（如 96°）；在 c 模式下，即便速度误差很大，PID 输出的 Uq 依然卡死在 195 无法上升。
- 根因A (锁死): 电机 UVW 物理相线产生的旋转磁场方向，与 AS5600 编码器的读数增加方向相反，导致坐标变换形成正反馈互相卡死。
- 根因B (限幅): PID 的积分累加器上限 (max_int) 初始化为 50000。由于算法内部 Q8.8 格式的右移 8 位操作 (>> 8)，导致最终 Q15 格式的实际输出上限被死死卡在了 195 (50000 / 256)，系统被严重“限流”。
- 修复: 
  1. 硬件调整: 随意对调电机的任意两根相线 (如 U 和 V) 以反转磁场方向。
  2. 软件调整: 将 AlgPid_SetGains 中的积分上限参数从 50000 暴力调大至 2000000，释放积分器的输出空间 (最高可达约 7812 的 Q15 电压)。

